[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "shogun"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pygame>=2.5.0"]

[project.scripts]
shogun = "shogun.main:main"

[tool.setuptools.packages.find]
where = ["src"]
